# ============= OS 잡파일 =============
# macOS / Windows
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# ============= IDE / 에디터 =============
# VS Code
.vscode/
# IntelliJ / Android Studio
.idea/
*.iml
out/
# Eclipse (혹시 쓸 때)
.project
.classpath
.settings/

# ============= Node.js / Frontend =============
node_modules/
# 빌드 산출물
dist/
build/
public/build/
public/dist/
# 프레임워크 캐시/출력
.next/
.nuxt/
.svelte-kit/
.storybook-out/
# 번들러/도구 캐시
.cache/
.parcel-cache/
.eslintcache
.stylelintcache
.turbo/
# Vite 내부 캐시(버전에 따라 생성 위치 다름)
vite.svg
vite.config.ts.timestamp-*
# 패키지 매니저 캐시
.pnpm-store/
.yarn/*
!.yarn/patches/
!.yarn/releases/
!.yarn/plugins/
!.yarn/sdks/
!.yarn/versions/
# 테스트/커버리지
coverage/
.nyc_output/
jest-html-reporters-*/ 
reports/
# 임시 로컬 설정
.env
.env.*
!.env.example

# ============= Java / Kotlin (JDK, Maven, Gradle) =============
# 공통 빌드 산출물
target/
build/
bin/
*.class
*.log

# Gradle
.gradle/
# Gradle Wrapper는 커밋 유지 (gradlew, gradlew.bat, gradle/wrapper/**) ⇒ 제외하지 말 것

# Maven
.mvn/timing.properties
# Maven Wrapper 역시 커밋 유지 (mvnw, mvnw.cmd, .mvn/wrapper/**)

# Spring Boot
# 임시 파일/로컬 설정
*.h2.db
*.mv.db
*.h2.sql
application-local.yml
application-local.properties

# ============= 자바스크립트 도구별 추가 =============
# Cypress/Playwright 등
cypress/videos/
cypress/screenshots/
playwright-report/
test-results/

# ============= 이미지/미디어(선택) =============
# 대용량 임시 파일은 추적 비권장
*.psd
*.ai
*.sketch

# ============= 인증서/키/보안(강력 권장) =============
*.pem
*.key
*.crt
*.pfx
*.p12
*.jks
*.keystore
*.der
*.cer
# 민감 설정
**/secrets.*
**/secret.*
**/*credentials*
**/*credential*
**/*password*
**/*token*

# ============= 압축/덤프(선택) =============
*.zip
*.tar
*.tar.gz
*.7z
*.rar
*.bak
*.tmp
*.swp
*.orig

# ============= 로그/프로파일링(선택) =============
logs/
*.log
*.pid
*.seed
heapdump.hprof

# ============= OS별 실행파일(선택) =============
*.exe
*.dll
*.so
*.dylib
